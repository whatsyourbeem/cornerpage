# 디자인 가이드 (톤별 시스템)

> 이 문서는 콘텐츠 쪽 문서들(정의·블록구조·입력문항·JSON스키마)과 짝을 이루는 디자인 쪽 문서입니다. 콘텐츠가 "무엇을 담을지"를 정의했다면, 이 문서는 "어떻게 보여줄지"를 정의합니다. [One Page Love](https://onepagelove.com/inspiration) 실제 사례 조사를 디자인 방향 설정에 적극 반영했습니다.

## 0. 왜 이 문서가 필요한가

유저가 "좋은 사이트"라고 직관적으로 느끼는 가장 큰 요소는 디자인입니다. 콘텐츠가 아무리 원칙에 맞아도 디자인이 템플릿처럼 보이면 신뢰가 깎입니다. 하지만 코드를 먼저 짜면 "느낌 가는 대로" 만들게 되고, 콘텐츠 쪽에서 조심했던 "업체마다 품질이 들쭉날쭉해지는" 문제가 디자인에서 재발합니다. 그래서 콘텐츠와 똑같은 순서를 따릅니다: **7원칙 → 톤별 디자인 토큰 → 블록별 렌더링 규칙**.

---

## 1. 7대 원칙의 시각적 번역

SKILL.md의 7원칙을 렌더러가 지켜야 할 구체적 규칙으로 옮깁니다.

| 원칙 | 시각적 규칙 |
|---|---|
| 명료성 | 히어로 헤드라인 최소 28px(모바일), 배경과 텍스트 명도 대비 4.5:1 이상. 첫 화면(뷰포트)에 배지·헤드라인·CTA가 스크롤 없이 다 보여야 함. |
| 로컬 앵커링 | 지역명이 담긴 배지는 항상 히어로 최상단, 폰트 크기를 헤드라인보다 작게 하되 위치는 가장 먼저 읽히는 자리(상단). |
| 의도완결성 | 메뉴·위치·리뷰·FAQ 등 서로 다른 블록이 각각 완결된 카드/섹션으로 시각 분리(구분선·여백)되어, 방문자가 원하는 정보로 스캔하며 건너뛸 수 있어야 함. |
| 신뢰 | 실제 사진은 필터·과도한 보정 없이. 스톡처럼 보이는 과도하게 완벽한 구도·인물 사진 지양. 신뢰스트립 숫자는 굵게, 큼직하게. |
| 마찰최소화 | 모든 CTA 버튼 터치 영역 44×44px 이상. 주 CTA는 페이지 내내(하단 고정바) 항상 도달 가능. 버튼 색은 배경과 최소 3:1 대비. |
| 집중 | 상단 고정바에 메뉴 내비게이션(햄버거 등) 넣지 않음. 외부 링크는 정보 블록에만 몰아서. 광고·배너·불필요한 팝업 없음. |
| 발견·속도 | 이미지 lazy-load, 3G 환경에서도 첫 화면 2초 이내 렌더링 목표. 반응형 필수(모바일 우선 설계, 데스크톱 폭 규칙은 6장 참조). |

---

## 2. One Page Love 리서치 반영

실제 사례를 살펴보며 얻은 핵심 교훈 세 가지:

**교훈 1 — 타이포그래피는 "개성있는 디스플레이체 + 중립적 본문체" 조합이 정석이다.** 마드리드의 마들렌·커피숍 Madie는 Cottorway·Dazzle(개성있는 디스플레이) + Inter(중립 본문)를 조합했다. 우리도 한글 컨텍스트에서 같은 원리를 적용한다 — 헤드라인엔 성격 있는 서체, 본문·UI엔 가독성 높은 중립 서체.

**교훈 2 — "신뢰"의 표현은 업종 성격에 따라 완전히 다르다.** Finance 카테고리를 보면 죄다 다크모드+테크 느낌의 SaaS 스타일이다. 하지만 이건 글로벌 핀테크 스타트업의 신뢰 표현이지, 우리 신뢰형(장례용품·치과·법률 같은 로컬 서비스업)의 신뢰 표현과는 다르다. 우리 고객은 젊은 테크 투자자가 아니라 동네 주민이다. **우리의 신뢰형은 "차갑고 테크니컬한 신뢰"가 아니라 "따뜻하고 정중한 신뢰"**여야 한다 — 네이비·브론즈 같은 차분하지만 온기 있는 팔레트가 다크모드+네온보다 적합하다.

**교훈 3 — AI 생성 디자인의 "티 나는 기본값"을 피해야 한다.** frontend-design 가이드가 명시적으로 경고하는 3가지 클러스터: (1) 웜크림 배경(#F4F1EA 근처)+하이컨트라스트 세리프+테라코타 액센트(#D97757 근처 — 공교롭게도 Claude 브랜드색과 겹쳐서 더 티가 남), (2) 거의 검정 배경+단일 형광 그린/버밀리언 액센트, (3) 헤어라인 룰+각없는 브로드시트 레이아웃. 우리 톤 시스템을 설계할 때 이 세 클러스터를 의식적으로 피한다.

---

## 3. 톤별 디자인 토큰 시스템 (축 A)

이미 손으로 만든 3개 샘플(밀물다방·삼보토탈·네일오브윤)에서 검증된 팔레트를 공식 프리셋으로 승격하고, 혼합형 프리셋을 새로 정의합니다.

### 3-1. 감성형 (Warm/Emotional)
- **색상**: `--brand: #7a5c3e`(로스팅 브라운) `--brand-deep: #5c4530` `--accent: #c26b3a`(구리 오렌지) `--paper: #f7f3ec`(웜 크림, frontend-design 경고색과는 톤 차이를 둬 채도 낮춤) `--ink: #2b2622`
- **타이포**: 헤드라인 — 두껍고 따뜻한 고딕(예: Pretendard Bold, 자간 좁게). 본문 — Pretendard Regular.
- **모션**: 스크롤 시 부드러운 페이드업. 히어로 배경 살짝 패럴랙스. 과하지 않게(2~3개 포인트에만).
- **CTA 스타일**: 라운드 코너(12px), 채도 높은 액센트색 배경, 그림자로 입체감.
- **시그니처 요소**: 히어로 하단 곡선(wavy line) 구분선 — Madie에서 확인된 장치. 카페·베이커리류에 자연스럽게 어울림.

### 3-2. 신뢰형 (Calm/Credible) — "차갑지 않게"
- **색상**: `--brand: #2e4756`(차분한 네이비) `--brand-deep: #22343f` `--accent: #86754f`(절제된 브론즈골드) `--paper: #f4f5f6` `--ink: #23272b`
- **타이포**: 헤드라인 — 중간 굵기 고딕(과장 없는 안정감). 본문 — 동일 계열 Regular. 세리프 사용 금지(전통 신뢰 시도는 오히려 예스러움으로 읽힘, 우리 타겟은 실용적 신뢰).
- **모션**: 최소화. 페이드인 정도만, 패럴랙스·바운스 등 장식적 모션 지양(신뢰형에 과한 모션은 경박해 보임).
- **CTA 스타일**: 각진 코너(8px 이하), 단색 배경, 그림자 최소.
- **시그니처 요소**: 신뢰 스트립의 절제된 숫자 타이포(굵고 크게, 골드 포인트 컬러로만 강조). 사진 폴백 시 도트 패턴 텍스처.

### 3-3. 혼합형 (Driven/Balanced) — 전문성+동기부여
- **색상**: `--brand: #2d4a43`(딥 그린틸) `--brand-deep: #1f3630` `--accent: #e0793c`(에너제틱 코랄오렌지) `--paper: #f5f4f0` `--ink: #24211c`
  (주의: 검정+네온그린 조합은 AI 티 클러스터라 의식적으로 회피. 그린틸은 어둡지만 검정이 아니고, 액센트는 형광이 아닌 따뜻한 코랄로 톤을 낮춤)
- **타이포**: 헤드라인 — 굵고 임팩트 있는 고딕, 살짝 큰 자간. 본문 — 중립 고딕.
- **모션**: 숫자 카운트업 애니메이션(신뢰스트립), 스크롤 시 카드 순차 등장. 감성형보다 강하고 신뢰형보다 약한 중간 강도.
- **CTA 스타일**: 라운드(10px), 그라디언트 살짝(brand→accent), 강조감 있게.
- **시그니처 요소**: 강점 카드 그리드가 하나씩 순차 등장(스크롤 트리거).

### 3-4. 브랜드 컬러 오버라이드 (`meta.brand_color`)

사장님이 이미 쓰고 있는 브랜드 컬러(간판·로고 등)가 있으면, 톤 프리셋의 기본 팔레트 대신 그 색을 반영해야 합니다. 단, 이 값은 **프론트엔드가 컬러피커·로고 자동추출로 이미 확정해서 넘긴 hex 값**입니다 — 스킬(LLM)은 색상을 생성하거나 판단하지 않고, 콘텐츠 JSON의 `meta.brand_color`에 그 값을 그대로 통과시킬 뿐입니다. 렌더러가 실제 팔레트 계산을 담당합니다.

**렌더러가 해야 할 일:**

1. `meta.brand_color`가 `null`이면 → 3-1~3-3의 톤 프리셋 기본 팔레트를 그대로 사용.
2. `meta.brand_color`가 값이 있으면 → 그 hex를 "씨앗 색"으로 삼아, 코드가 결정적으로(deterministic) 전체 팔레트를 유도:
   - `--brand`: 입력받은 hex 그대로
   - `--brand-deep`: 명도를 낮춘 진한 버전(예: HSL 변환 후 lightness -15~20%)
   - 버튼 텍스트 색: `--brand` 위에서 흰색/검정 중 대비비가 더 높은 쪽을 자동 선택
   - **WCAG 대비 검증**: 배경-텍스트 조합이 4.5:1 미만이면, 색조(hue)는 유지한 채 명도만 조정해 기준을 통과시킴(사장님이 지정한 "느낌"은 보존하되 가독성은 보장)
3. **타이포·모션·시그니처 요소 등 구조적 토큰은 `axis_a_tone` 기준을 그대로 유지**합니다. 브랜드 컬러는 팔레트만 교체하고, 톤이 규정하는 "성격"(감성형의 부드러운 모션, 신뢰형의 절제된 모션 등)은 바뀌지 않습니다.

**왜 프론트엔드가 아니라 렌더러가 최종 계산을 하는가**: 컬러피커·로고추출은 "씨앗 색 하나"를 정하는 것까지만 프론트엔드 책임입니다. 그 씨앗에서 안전한 전체 팔레트(진한 버전, 대비 보정 등)를 만드는 건 `axis_a_tone`과 마찬가지로 렌더링 시점의 결정적 로직이어야 일관성이 보장됩니다.

**색을 지정하지 않은 경우(`null`)가 왜 "프론트엔드가 임의로 기본색을 넣는 것"이면 안 되는가**: 색상 입력 시점엔 아직 `axis_a_tone`(감성형/신뢰형/혼합형)이 확정되지 않았습니다(이건 스킬이 사업 정보 전체를 보고 판단). 프론트엔드가 이때 임의의 기본색을 넣으면, 나중에 확정된 톤과 색이 서로 안 맞을 위험이 있습니다. 그래서 미지정 시엔 반드시 `null`을 유지하고, 톤이 확정되는 렌더링 시점에 그 톤의 기본 팔레트를 적용해야 합니다.

### 3-5. 구현 패턴 예시 (참고용 의사코드)

`axis_a_tone` 하나가 테마 설정 전체를 선택하고, `brand_color`가 있으면 그 팔레트의 브랜드 컬러 부분만 오버라이드하는 구조입니다. 블록 컴포넌트(Hero, Menu 등)는 톤이 뭔지 몰라도 되고, CSS 변수만 참조하면 됩니다 — 톤별 분기가 컴포넌트 안에 흩어지지 않습니다.

```typescript
// 1. 톤별 기본 팔레트 (TypeScript가 3개 톤 전부 정의됐는지 컴파일 타임에 강제)
const TONE_CONFIG: Record<AxisATone, ToneConfig> = {
  "감성형": { cssVars: { "--brand": "#7a5c3e", "--accent": "#c26b3a", /* ... */ }, ctaRadius: "12px", motionIntensity: "moderate", signatureElement: "wavy-divider" },
  "신뢰형": { cssVars: { "--brand": "#2e4756", "--accent": "#86754f", /* ... */ }, ctaRadius: "8px", motionIntensity: "minimal", signatureElement: "dot-texture" },
  "혼합형": { cssVars: { "--brand": "#2d4a43", "--accent": "#e0793c", /* ... */ }, ctaRadius: "10px", motionIntensity: "moderate", signatureElement: "counter-animation" },
};

// 2. brand_color가 있으면 팔레트만 오버라이드(구조적 토큰은 톤 그대로 유지)
function resolveTone(meta: Meta): ToneConfig {
  const base = TONE_CONFIG[meta.axis_a_tone];
  if (!meta.brand_color) return base;
  return { ...base, cssVars: { ...base.cssVars, ...deriveColorPalette(meta.brand_color) } };
  // deriveColorPalette: 씨앗 hex → { --brand, --brand-deep, --button-text } 등 계산 + WCAG 대비 보정
}

// 3. 최상위 렌더러에서 한 번만 적용
function PageRenderer({ content }: { content: MiniHomepageContent }) {
  const tone = resolveTone(content.meta);
  return (
    <div style={tone.cssVars as React.CSSProperties}>
      <ToneContext.Provider value={tone}>{/* 블록들 */}</ToneContext.Provider>
    </div>
  );
}
```

색상·타이포처럼 CSS 변수로 되는 건 여기서 끝나지만, "감성형에만 히어로 하단에 곡선 구분선을 그린다" 같은 **구조적 결정**(장식 요소 유무·모션 강도·CTA 모서리 반경)은 CSS 변수만으론 안 됩니다. 이런 건 `ToneContext`로 내려줘서 블록 컴포넌트가 필요할 때 참조합니다(`tone.signatureElement === "wavy-divider"` 조건부 렌더링 등).

---

## 4. 레이아웃 규칙 (축 B)

블록 순서는 콘텐츠 스키마의 `axis_b_layout` 값을 그대로 렌더링 순서로 변환합니다.

- **갤러리우선**: `topbar → hero → trust_strip → gallery → menu → about → reviews → how_it_works → faq → info → sticky_cta`
- **메뉴우선**: `topbar → hero → trust_strip → about → menu → gallery → reviews → how_it_works → faq → info → sticky_cta`
- **해당없음**: gallery 블록 자체를 렌더링 트리에서 제외(공간을 차지하지 않음, 다른 블록이 그 자리로 당겨짐)

---

## 5. Null 폴백의 시각적 규칙

콘텐츠 스키마의 "null = 블록 오프" 원칙을 렌더러가 구현하는 방법입니다. 폴백을 각 개발자가 다르게 구현하면 페이지 품질이 들쭉날쭉해지므로 여기서 통일합니다.

| Null 필드 | 폴백 렌더링 |
|---|---|
| hero.background_image_url | 톤별 브랜드 단색 배경 + 은은한 도트/그레인 텍스처(순수 단색은 밋밋해 보임) + 큰 타이포 |
| logo_url | 가게명 텍스트를 로고타입 스타일로(굵게, 자간 조정, 톤별 브랜드 컬러) |
| about (블록 전체) | 블록 자체를 DOM에서 제거, 다음 블록이 자연스럽게 이어짐(빈 여백 남기지 않음) |
| gallery (블록 전체) | 블록 제거. 갤러리우선 레이아웃이었어도 메뉴가 그 자리로 |
| reviews / how_it_works / faq | 블록 제거 |
| menu.items[].image_url | 이미지 없는 리스트형으로(카드가 아니라 텍스트 행), 사진 있는 항목과 없는 항목이 섞이지 않게 전체를 리스트형으로 통일 |

**원칙: 폴백은 "빈 공간"이 아니라 "다른 스타일로 완결된 대안"이어야 한다.** 빈 칸처럼 보이면 미완성으로 느껴져 신뢰가 깎인다.

---

## 6. 접근성 · 기술 품질 기준

- **반응형 폭 규칙 (카드 정체성 + 데스크톱 활용의 절충안)**: 320px(소형 모바일)부터 1280px(데스크톱)까지 대응하되, 완전 고정도 완전 반응형도 아닌 중간 지점을 택한다.
  - **카드(콘텐츠) 폭**: 모바일 480px → 데스크톱 640~720px로 소폭 확장. 완전히 다른 데스크톱 전용 레이아웃은 만들지 않는다 — 미니홈페이지 정체성(명함·Linktree류처럼 압축된 단일 컬럼)을 유지하기 위함이며, 텅 빈 느낌은 줄이는 절충.
  - **풀블리드 허용 요소(카드 폭을 벗어나 화면 전체 폭으로 확장 가능)**:
    - `hero.background_image_url` 배경 이미지 — 데스크톱에서 시원한 임팩트를 위해 풀블리드. 단 텍스트·CTA는 카드 폭 안에 정렬.
    - `topbar`(상단 고정바) — 바 자체는 풀블리드, 내부 콘텐츠(가게명·액션버튼)는 카드 폭에 맞춰 정렬해 스크롤 경계가 자연스럽게 이어지도록.
    - `gallery` 그리드 — 모바일 2열 → 데스크톱 3~4열로 컬럼 수 확장(사진이 많을수록 유리한 유일한 블록).
  - **카드 폭 유지 요소**: 위 세 가지를 제외한 모든 블록(히어로 텍스트, 신뢰스트립, 소개, 메뉴, 리뷰, 정보, FAQ, 하단 CTA바 등)은 데스크톱에서도 카드 폭 안에 머문다. 이게 가독성과 "명함처럼 압축된" 인상을 지켜준다.
  - 데스크톱 화면에서 카드 바깥 여백은 미묘한 배경색(순백색이 아닌 살짝 톤 낮춘 배경)으로 채워, 카드가 위에 "떠 있는" 느낌을 준다.
- 키보드 포커스: 모든 인터랙티브 요소에 명확한 포커스 링(outline) 표시.
- `prefers-reduced-motion` 존중: 모션 감소 설정 시 모든 애니메이션·패럴랙스 비활성화.
- 색상 대비: 텍스트 4.5:1, 큰 텍스트(24px+) 3:1 이상(WCAG AA).
- 이미지 alt 텍스트: 콘텐츠 스키마 확장 시점(백로그)에 필드가 추가되면 렌더러가 필수로 사용하도록 미리 구조 대비.

### 6-1. 데스크톱 채움 기법 (카드 폭 규칙만으로는 부족할 때)

실측 검증(1512px 데스크톱 프리뷰) 결과, 카드 폭을 640~720px로 넓히는 것만으로는 넓은 화면에서 "휑해 보이는" 인상이 완전히 해소되지 않았다. 아래 기법을 **우선순위 순서대로** 적용한다. 1~2번은 필수, 3번 이후는 권장.

**① 섹션별 풀블리드 배경띠 (최우선 · 모든 섹션에 적용)**

카드 바깥을 밋밋한 단색으로 두지 말고, **섹션마다 배경색을 화면 전체 폭으로 채우고 텍스트만 카드 폭 안에 정렬**한다. 히어로에서 이미 하고 있는 "배경 풀블리드 + 텍스트 카드폭" 패턴을 신뢰스트립·정보 블록 등 배경색이 있는 모든 섹션으로 확장.

```css
.section {
  width: 100%;              /* 배경은 뷰포트 전체 폭 */
  padding: 48px 24px;
}
.section-inner {
  max-width: 720px;         /* 텍스트 콘텐츠만 카드 폭으로 제한 */
  margin: 0 auto;
}
/* 톤별 섹션 배경색 예시 (신뢰스트립) */
.trust-strip { background: var(--brand-deep); }   /* 화면 끝까지 */
.about       { background: var(--paper-warm); }
.info        { background: var(--brand-deep); }
```
일반 콘텐츠 섹션(소개·메뉴·리뷰·FAQ)은 배경을 `--paper`(기본 배경)와 살짝 다른 톤(`--paper-warm` 등)으로 교대로 주면, 섹션 경계가 시각적으로 구분되면서 화면이 "줄무늬"처럼 채워져 허전함이 줄어든다.

**② 카드 그림자·프레임감 (필수)**

카드 전체를 감싸는 컨테이너에 은은한 그림자를 줘서, 좁은 폭이 "미완성"이 아니라 "의도된 모바일 프레임 룩"으로 읽히게 한다.

```css
@media (min-width: 768px) {
  .page-container {
    max-width: 720px;
    margin: 0 auto;
    box-shadow: 0 0 60px rgba(0, 0, 0, 0.08);
    min-height: 100vh;
  }
  body {
    background: #EDEDE9;   /* 카드 바깥 배경 — 순백색 금지, 살짝 톤 낮춘 뉴트럴 */
  }
}
```

**③ 톤별 장식 배경 (권장, 데스크톱 전용)**

카드 양옆 여백(`body` 배경)에 톤별로 다른 은은한 장식을 깔아 화면을 채운다. **카드 콘텐츠보다 절대 튀면 안 되며**, `opacity` 낮게 또는 매우 옅은 색상으로 처리.

```css
/* 감성형: 부드러운 원형 블러 글로우 */
body[data-tone="감성형"]::before {
  content: "";
  position: fixed; top: -10%; left: -10%;
  width: 40vw; height: 40vw;
  background: radial-gradient(circle, var(--accent) 0%, transparent 70%);
  opacity: 0.08;
  filter: blur(60px);
  z-index: -1;
}

/* 신뢰형: 거의 안 보이는 옅은 도트 패턴 */
body[data-tone="신뢰형"] {
  background-image: radial-gradient(var(--brand) 1px, transparent 1px);
  background-size: 24px 24px;
  background-color: #EDEDE9;
  background-blend-mode: normal;
}
/* 도트 자체의 색상 opacity를 5% 이하로: rgba(46,71,86,0.05) 형태로 --brand를 rgba 변환해 사용 */

/* 혼합형: 살짝 각진 기하학 도형 1~2개, 옅게 */
body[data-tone="혼합형"]::after {
  content: "";
  position: fixed; bottom: -5%; right: -5%;
  width: 30vw; height: 30vw;
  background: var(--accent);
  opacity: 0.06;
  clip-path: polygon(20% 0%, 100% 0%, 80% 100%, 0% 100%);
  z-index: -1;
}
```

**④ 리뷰 가로 캐러셀 (풀블리드, 권장)**

리뷰가 2개 이상이면 세로 스택 대신 가로 스크롤 캐러셀로 전환, 갤러리처럼 화면 폭을 활용한다.

```css
.reviews-track {
  width: 100%;
  display: flex;
  gap: 16px;
  overflow-x: auto;
  padding: 0 24px;
  scroll-snap-type: x mandatory;
}
.review-card {
  flex: 0 0 320px;
  scroll-snap-align: start;
}
```

**⑤ 콘텐츠 많은 블록의 2열 그리드 (선택, 업종별 판단)**

메뉴·FAQ 항목이 6개 이상으로 많은 업체(쇼케이스 샘플처럼 풀옵션인 경우)에 한해, 카드 폭을 800~900px로 예외 확장하고 2열 그리드를 적용할 수 있다. 단 이 경우 "명함처럼 압축된" 인상이 옅어지므로, **항목 수가 실제로 많을 때만** 조건부 적용하고 기본값으로 삼지 않는다.

```css
@media (min-width: 768px) {
  .menu-list.is-dense,     /* 항목 6개 이상일 때만 렌더러가 이 클래스 부여 */
  .faq-list.is-dense {
    max-width: 880px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px 24px;
  }
}
```

**적용 순서 요약**: ①②는 모든 페이지에 기본 적용 → ③은 톤 시스템과 함께 적용 → ④는 리뷰 2개 이상일 때 → ⑤는 항목이 실제로 많을 때만 조건부.

---

## 7. 다음 단계 (To-Do)

- [ ] 톤별 CSS 커스텀 프로퍼티(디자인 토큰) 파일로 formalize
- [ ] 블록별 React 컴포넌트 설계(컴포넌트 = content.types.ts의 각 블록 타입을 prop으로 받음)
- [ ] 3개 톤 프리셋으로 실제 프로토타입 렌더러 구축, 기존 6개 업종 콘텐츠 JSON으로 검증
- [ ] 7원칙 체크리스트를 렌더러 자체 QA 스크립트로 구현 가능한지 검토(대비율 자동 검사 등)
- [ ] 6-1 데스크톱 채움 기법(①②는 필수) 적용 후 7개 프리뷰 샘플 데스크톱 화면 재검토
